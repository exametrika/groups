{
	logging:
	{
		appenders:
		{
	    	console:
	   		{
	   			instanceOf = "ConsoleAppender"
	   			//template = "<%@template(default)%>"
	   			template = "<%@template(test)%>"
	   			colorize = false
	   		}
	   	}
	
		root:
		{
			level = "error"
			appenders: ["console"]
		}
		
		loggers:
		{
			/*com.exametrika.common.messaging.impl.protocols.trace.TracingProtocol.Low:
			{
				level = "trace"
				filters:["return message.contains('[test0]') and message.contains(', 20')"]
			}*/
			com.exametrika.impl.groups.core.discovery.DiscoveryProtocol:
			{
				level = "error"
			}
			com.exametrika.impl.groups.core.failuredetection.FailureDetectionProtocol:
			{
				level = "error"
			}
		}
   	}
}
